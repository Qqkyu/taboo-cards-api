import { FunctionComponent } from "react";

export const Header: FunctionComponent = () => {
  return (
    <div className="navbar bg-base-100">
      <button className="btn btn-ghost normal-case text-xl">Taboo cards API</button>
    </div>
  );
};
